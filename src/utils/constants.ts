export enum OrdersStatus {
  PENDING = "pending",
  COMPLETED = "completed",
  CANCELLED = "cancelled",
  DELIVERED = "delivered",
  DELIVERING = "delivering",
  DELETED = "deleted",
}

export enum OrdersTypes {
  DELIVERY = "delivery",
  PICKUP = "pickup",
}

export enum UserActions {
  CREATE = "create",
  UPDATE = "update",
  DELETE = "delete",
}
